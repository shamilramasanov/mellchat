{
  "buildCommand": "cd frontend/pwa && npm install && npm run build",
  "outputDirectory": "frontend/pwa/build",
  "installCommand": "echo 'Skip root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}