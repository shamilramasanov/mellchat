{
  "buildCommand": "npm install --prefix frontend/pwa && npm run build --prefix frontend/pwa",
  "outputDirectory": "frontend/pwa/build",
  "installCommand": "npm install --prefix frontend/pwa",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}