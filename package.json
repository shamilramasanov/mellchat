{
  "name": "mellchat",
  "version": "2.0.0",
  "description": "Real-time chat aggregator",
  "scripts": {
    "build": "cd frontend/pwa && npm install && CI=false npm run build",
    "start": "cd backend/api-gateway && npm install && npm start",
    "postinstall": "cd backend/api-gateway && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
