# 🔧 Railway: Nixpacks vs Dockerfile - КРИТИЧНО!

## Проблема

Railway **выбирает builder автоматически** по приоритету:
1. `nixpacks.toml` (если есть в корне)
2. `Dockerfile` (если есть в корне)
3. `package.json` (Nixpacks автоопределение)

## Что происходило

1. В корне **БЫЛ** `package.json` - Railway использовал Nixpacks
2. Nixpacks запускал `npm start` из **корня** репозитория
3. Команда `cd backend/api-gateway && npm run start:with-migrations` падала
4. Контейнер **бесконечно перезапускался**

## Решение

**Удалили корневой `package.json`**, чтобы Railway **использовал Dockerfile**.

## Проверка

После деплоя проверь логи:

```bash
# Должно быть:
Building Dockerfile...
# НЕ должно быть:
Using Nixpacks...
```

## Если Nixpacks все еще используется

**В Railway Dashboard:**
1. Settings → Builder → **"Dockerfile"**
2. Сохранить
3. Redeploy

---

**Главное:** Railway **НЕ ИЩЕТ `nixpacks.toml` в `backend/api-gateway/`!**

Он ищет только в **корне репозитория**!

Если нужен Nixpacks, создай `nixpacks.toml` в **корне** с правильными путями!

