# Логика фильтров "Вопросы" и "Все Вопросы" в MellChat

## 🎯 Общая концепция

Фильтры "Вопросы" и "Все Вопросы" позволяют пользователю быстро находить и просматривать вопросы из чатов стримеров. Это критически важно для стримеров, которые ведут трансляции на нескольких платформах одновременно (например, Twitch + YouTube + Kick).

## 📍 Расположение фильтров

**Компонент:** `BottomSearchBar.jsx` (`frontend/pwa/src/features/newui/components/BottomSearchBar.jsx`)

Фильтры находятся в выпадающем меню, которое открывается по нажатию на кнопку "+" в нижней панели поиска.

**Структура меню:**
```
+ (кнопка)
  ├─ AI
  ├─ Add Stream
  ├─ Вопросы ← Фильтр для текущего стрима
  ├─ Все Вопросы ← Фильтр для всех стримов
  └─ Moods
```

## 🔍 Фильтр "Вопросы"

### Назначение
Показывает только вопросы из **текущего активного чата** (стрима).

### Логика работы

1. **Область применения:** Только активный стрим (`activeStreamId`)
2. **Фильтрация:**
   ```javascript
   const filteredMessages = messages.filter(m => 
     m.streamId === activeStreamId && m.isQuestion === true
   );
   ```
3. **Отображение:**
   - Показывает только сообщения с флагом `isQuestion: true`
   - Только из текущего активного стрима
   - Сохраняется порядок по времени получения

### Поведение UI

- При активации фильтра:
  - Чат переключается на отображение только вопросов из активного стрима
  - Все остальные сообщения скрываются
  - Кнопка "Вопросы" в меню "+" должна иметь визуальную индикацию активного состояния (например, подсветка или галочка)

- При деактивации:
  - Чат возвращается к обычному режиму (все сообщения активного стрима)

### Пример использования

**Сценарий:** Пользователь смотрит чат стрима на Twitch, в котором много обычных сообщений и несколько вопросов.

1. Нажимает "+" → выбирает "Вопросы"
2. Чат фильтруется, показывая только вопросы из Twitch чата
3. Видит только вопросы, без лишних сообщений
4. Может нажать еще раз "Вопросы" для возврата к обычному режиму

---

## 🌐 Фильтр "Все Вопросы"

### Назначение
Показывает **все вопросы из всех активных стримов одновременно** в едином списке. Критически важно для стримеров, которые ведут трансляцию на нескольких платформах (мультиплатформенный стриминг).

### Логика работы

1. **Область применения:** Все активные стримы (`activeStreams`)
2. **Фильтрация:**
   ```javascript
   const filteredMessages = messages.filter(m => 
     activeStreams.some(stream => stream.id === m.streamId) && 
     m.isQuestion === true
   );
   ```
3. **Сортировка:**
   - По времени получения (хронологически, от старых к новым)
   - Вопросы из разных стримов перемешаны по времени
4. **Отображение источника:**
   - Каждый вопрос должен содержать метку/бейдж с указанием платформы/стрима
   - Например: `[Twitch]`, `[YouTube]`, `[Kick]`
   - Или иконку платформы рядом с вопросом

### Поведение UI

- При активации фильтра:
  - Чат показывает все вопросы из всех активных стримов
  - Вопросы из разных стримов перемешаны по времени
  - Каждый вопрос имеет визуальную индикацию источника (платформа/стрим)
  - Кнопка "Все Вопросы" в меню "+" имеет визуальную индикацию активного состояния

- При навигации по вопросу:
  - При клике на вопрос из другого стрима → автоматическое переключение на этот стрим (если он не активен)
  - Прокрутка к конкретному вопросу в чате соответствующего стрима

- При деактивации:
  - Возврат к обычному режиму (показ активного стрима)

### Пример использования

**Сценарий:** Стример ведет трансляцию одновременно на Twitch, YouTube и Kick. Вопросы приходят во все три чата.

1. Нажимает "+" → выбирает "Все Вопросы"
2. Видит единый список всех вопросов из всех трех платформ:
   ```
   [Twitch] User1: Какой ваш любимый герой?
   [YouTube] User2: Когда будет следующий стрим?
   [Kick] User3: Как настроить OBS?
   [Twitch] User4: Покажешь новую игру?
   [YouTube] User5: Сколько часов стримишь в день?
   ```
3. Все вопросы отсортированы по времени, независимо от платформы
4. Может видеть вопросы из всех платформ одновременно, не переключаясь между стримами
5. При клике на вопрос из YouTube → автоматически переключается на YouTube стрим и прокручивается к этому вопросу

---

## 🔄 Взаимодействие фильтров

### Правила переключения

1. **Взаимоисключающие:**
   - При активации "Вопросы" → "Все Вопросы" деактивируется
   - При активации "Все Вопросы" → "Вопросы" деактивируется
   - Может быть активен только один фильтр за раз

2. **Возврат к обычному режиму:**
   - Повторное нажатие на активный фильтр → деактивация, возврат к обычному режиму
   - Или активация другого фильтра (например, "Moods")

3. **Сохранение состояния при переключении стримов:**
   - Если активен фильтр "Все Вопросы" → остается активным при переключении между стримами
   - Если активен фильтр "Вопросы" → при переключении стрима показываются вопросы нового активного стрима

---

## 🔍 Интеграция с поиском

### Работа поиска с фильтрами

**Когда активен фильтр "Вопросы":**
- Поиск работает **только среди вопросов** текущего активного стрима
- Поисковый запрос фильтрует уже отфильтрованные вопросы

**Когда активен фильтр "Все Вопросы":**
- Поиск работает **только среди всех вопросов** из всех активных стримов
- Поисковый запрос фильтрует объединенный список вопросов

**Пример:**
1. Активен фильтр "Все Вопросы" → видны все вопросы из всех стримов
2. Вводим в поиск "OBS" → показываются только вопросы, содержащие "OBS", из всех стримов

---

## 📊 Визуальная индикация активного фильтра

### Критически важно: пользователь должен четко видеть, какой фильтр активен

### Индикация в меню "+"

**Кнопка активного фильтра должна быть визуально выделена:**

1. **Цветовое выделение:**
   - **Активный фильтр "Вопросы":**
     - Фон: `bg-orange-100` или `bg-orange-50` (оранжевый, как иконка MessageSquare)
     - Текст: `text-orange-700` или `text-orange-600`
     - Иконка: `text-orange-500`
     - Граница: `border-2 border-orange-300`
   
   - **Активный фильтр "Все Вопросы":**
     - Фон: `bg-green-100` или `bg-green-50` (зеленый, как иконка Filter)
     - Текст: `text-green-700` или `text-green-600`
     - Иконка: `text-green-500`
     - Граница: `border-2 border-green-300`

2. **Дополнительные визуальные маркеры:**
   - Иконка галочки (`Check` из lucide-react) справа от текста
   - Или изменение иконки (например, заливка вместо контура)
   - Жирный шрифт: `font-semibold` или `font-bold`

3. **Неактивное состояние:**
   - Обычный серый фон: `bg-white` или `hover:bg-gray-100`
   - Серый текст: `text-black` или `text-gray-700`
   - Без границы или тонкая серая граница

**Пример реализации:**
```jsx
// Активный фильтр "Вопросы"
<button className={`w-full flex items-center space-x-3 px-4 py-3 transition-colors border-2 ${
  activeFilter === 'questions' 
    ? 'bg-orange-100 text-orange-700 border-orange-300 font-semibold' 
    : 'bg-white text-black border-gray-200 hover:bg-gray-100'
}`}>
  <MessageSquare className={`h-4 w-4 ${activeFilter === 'questions' ? 'text-orange-500' : 'text-orange-500'}`} />
  <span className="text-sm flex-1">{t('filters.questions')}</span>
  {activeFilter === 'questions' && <Check className="h-4 w-4 text-orange-600" />}
</button>
```

### Индикация в чате

**Баннер/индикатор вверху чата (обязательно!):**

1. **Баннер активного фильтра:**
   - Расположение: вверху `ChatContainer`, над списком сообщений
   - Цвет соответствует активному фильтру:
     - Для "Вопросы": оранжевый акцент (`bg-orange-50 border-orange-200`)
     - Для "Все Вопросы": зеленый акцент (`bg-green-50 border-green-200`)
   
2. **Содержимое баннера:**
   - Иконка фильтра слева
   - Текст: "Показываются только вопросы" или "Показываются вопросы из всех стримов"
   - Счетчик: количество отфильтрованных вопросов (например, "15 вопросов")
   - Кнопка "Очистить фильтр" (иконка X или "×") справа
   - При клике на "Очистить" → фильтр деактивируется

3. **Стили баннера:**
```jsx
// Для фильтра "Вопросы"
<div className="flex items-center justify-between px-4 py-2 bg-orange-50 border-b border-orange-200">
  <div className="flex items-center space-x-2">
    <MessageSquare className="h-4 w-4 text-orange-600" />
    <span className="text-sm text-orange-800 font-medium">
      Показываются только вопросы ({filteredMessages.length})
    </span>
  </div>
  <button onClick={clearFilter} className="p-1 hover:bg-orange-100 rounded">
    <X className="h-4 w-4 text-orange-600" />
  </button>
</div>

// Для фильтра "Все Вопросы"
<div className="flex items-center justify-between px-4 py-2 bg-green-50 border-b border-green-200">
  <div className="flex items-center space-x-2">
    <Filter className="h-4 w-4 text-green-600" />
    <span className="text-sm text-green-800 font-medium">
      Показываются вопросы из всех стримов ({filteredMessages.length})
    </span>
  </div>
  <button onClick={clearFilter} className="p-1 hover:bg-green-100 rounded">
    <X className="h-4 w-4 text-green-600" />
  </button>
</div>
```

4. **Анимация появления:**
   - Плавное появление баннера при активации фильтра
   - Можно использовать `framer-motion` для плавной анимации slideDown

### Отображение источника вопроса

**Условия показа бейджа платформы:**
- Активен фильтр "Все Вопросы" (`activeFilter === 'allQuestions'`)
- ИЛИ активно более одного стрима одновременно (`activeStreams.length > 1`)

**Условия показа бейджа канала/автора:**
- Бейдж канала показывается **всегда**, когда доступна информация о стриме (`streamInfo.channelName`)
- Позиция: **справа от имени пользователя**

**Отображение элементов:**
1. **Бейдж платформы** (если условие выполнено):
   - TWITCH — фиолетовый (`bg-purple-100 text-purple-700`)
   - YOUTUBE — красный (`bg-red-100 text-red-700`)
   - KICK — зеленый (`bg-lime-100 text-lime-700`)

2. **Иконка платформы** (если бейдж платформы скрыт):
   - Маленькая SVG иконка соответствующей платформы

3. **Имя пользователя** — жирный черный текст

4. **Бейдж канала/автора** (всегда, если доступен):
   - Синий бейдж (`bg-blue-100 text-blue-700 border border-blue-200`)
   - Содержит название канала (например, "ramzes", "deko")
   - Позиция: **справа от имени пользователя**

**Пример отображения:**
```
[TWITCH] username [ramzes]
  или
[иконка] username [ramzes]
```

**Техническая реализация:**
```jsx
// В MessageCard.jsx
<div className="flex items-center space-x-2 flex-wrap gap-1">
  {showPlatformBadge && streamInfo && (
    <span className="px-2 py-0.5 rounded text-xs font-medium bg-purple-100 text-purple-700">
      {streamInfo.platform.toUpperCase()}
    </span>
  )}
  {!showPlatformBadge && platformIcons[message.platform]}
  <span className="font-semibold text-black">{message.username}</span>
  {/* Бейдж канала - справа от имени */}
  {streamInfo && streamInfo.channelName && (
    <span className="px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-700 border border-blue-200">
      {streamInfo.channelName}
    </span>
  )}
</div>
```

**Функция получения информации о стриме:**
```jsx
// В ChatContainer.jsx
const getStreamInfo = (messageStreamId) => {
  const stream = activeStreams?.find(s => s.id === messageStreamId);
  if (!stream) {
    // Fallback: извлекаем из messageStreamId (формат "platform-channelname")
    const parts = messageStreamId.split('-');
    if (parts.length > 1) {
      return { platform: parts[0], channelName: parts.slice(1).join('-') };
    }
    return { platform: 'unknown', channelName: null };
  }
  
  // Приоритет извлечения названия канала:
  // 1. stream.streamId (чистое название канала)
  // 2. stream.title
  // 3. Из stream.id (формат "platform-channelname")
  let channelName = stream.streamId || stream.title;
  if (!channelName && stream.id) {
    const parts = stream.id.split('-');
    if (parts.length > 1) {
      channelName = parts.slice(1).join('-');
    }
  }
  
  return {
    platform: stream.platform || 'unknown',
    channelName: channelName || null
  };
};
```

**Важные детали реализации:**
- `streamInfo` всегда передается в `MessageCard`, даже если бейдж платформы скрыт
- Название канала извлекается из `stream.streamId`, `stream.title` или `stream.id`
- Если стрим не найден в `activeStreams`, используется fallback извлечение из `messageStreamId`
- Бейдж канала всегда показывается, когда доступен `streamInfo.channelName`

---

## 📈 Счетчики и статистика

### Отображение количества

**В меню "+":**
- Кнопка "Вопросы" может показывать количество непрочитанных вопросов в активном стриме:
  ```
  Вопросы (5)
  ```
- Кнопка "Все Вопросы" может показывать общее количество непрочитанных вопросов во всех стримах:
  ```
  Все Вопросы (12)
  ```

**В чате:**
- В заголовке/баннере можно показать общее количество отфильтрованных вопросов:
  - "Показываются 15 вопросов" (для "Все Вопросы")
  - "Показываются 3 вопроса" (для "Вопросы" в активном стриме)

---

## 🎯 Технические детали реализации

### Состояние фильтра

**Рекомендуемая структура:**
```javascript
// В AppNewUI или глобальном состоянии
const [activeFilter, setActiveFilter] = useState(null);
// null - обычный режим
// 'questions' - фильтр "Вопросы" для активного стрима
// 'allQuestions' - фильтр "Все Вопросы" для всех стримов
```

### Фильтрация сообщений

**Для "Вопросы":**
```javascript
const filteredMessages = useMemo(() => {
  if (activeFilter === 'questions' && activeStreamId) {
    return allMessages.filter(m => 
      m.streamId === activeStreamId && m.isQuestion === true
    );
  }
  return messagesForActive; // Обычный режим
}, [activeFilter, activeStreamId, allMessages]);
```

**Для "Все Вопросы":**
```javascript
const filteredMessages = useMemo(() => {
  if (activeFilter === 'allQuestions') {
    const activeStreamIds = activeStreams.map(s => s.id);
    return allMessages.filter(m => 
      activeStreamIds.includes(m.streamId) && m.isQuestion === true
    ).sort((a, b) => a.timestamp - b.timestamp); // Сортировка по времени
  }
  return messagesForActive; // Обычный режим
}, [activeFilter, activeStreams, allMessages]);
```

### Навигация по вопросам

**При клике на вопрос в режиме "Все Вопросы":**
```javascript
const handleQuestionClick = (questionMessage) => {
  // Если вопрос из другого стрима - переключаемся на него
  if (questionMessage.streamId !== activeStreamId) {
    switchStream(questionMessage.streamId);
  }
  
  // Прокручиваем к вопросу
  setTargetMessageId(questionMessage.id);
};
```

---

## 📝 Закрытые и свернутые стримы

### Вопрос: Включать ли вопросы из закрытых/свернутых стримов?

**Рекомендация:** НЕТ

**Логика:**
- Фильтр "Все Вопросы" должен показывать вопросы только из **активных стримов** (`activeStreams`)
- Закрытые стримы (`closedStreamIds`) не подключены к платформам и не получают новые сообщения
- Свернутые стримы (`collapsedStreamIds`) продолжают получать сообщения, но они скрыты из UI

**Вариант реализации:**
```javascript
const filteredMessages = allMessages.filter(m => {
  // Только из активных стримов
  const isFromActiveStream = activeStreams.some(s => s.id === m.streamId);
  // Не из закрытых
  const isNotClosed = !closedStreamIds.includes(m.streamId);
  // Вопросы
  const isQuestion = m.isQuestion === true;
  
  return isFromActiveStream && isNotClosed && isQuestion;
});
```

**Альтернативный вариант (по требованию):**
- Можно добавить настройку: "Включать вопросы из свернутых стримов"
- По умолчанию: только активные стримы

---

## ✅ Чеклист реализации

- [ ] Добавить состояние `activeFilter` в AppNewUI
- [ ] Реализовать фильтрацию сообщений для "Вопросы"
- [ ] Реализовать фильтрацию сообщений для "Все Вопросы"
- [ ] Добавить визуальную индикацию активного фильтра в меню "+"
- [ ] Добавить баннер/индикатор активного фильтра в чате
- [ ] Реализовать отображение метки платформы для вопросов в режиме "Все Вопросы"
- [ ] Реализовать автоматическое переключение стрима при клике на вопрос из другого стрима
- [ ] Добавить счетчики вопросов в меню "+"
- [ ] Интегрировать поиск с фильтрами
- [ ] Обработать деактивацию фильтра (повторное нажатие)
- [ ] Протестировать работу фильтров при переключении стримов
- [ ] Добавить локализацию для всех текстов

---

*Документация создана: 2025-01-31*

