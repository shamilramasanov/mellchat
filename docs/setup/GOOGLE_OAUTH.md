# Настройка Google OAuth авторизации

## 🎯 Что это дает

Пользователи смогут входить в приложение через свой Google аккаунт без ввода email и кода подтверждения.

## 📋 Пошаговая инструкция

### Шаг 1: Создайте проект в Google Cloud Console

1. **Откройте Google Cloud Console:**
   - Перейдите на https://console.cloud.google.com
   - Войдите в свой Google аккаунт

2. **Создайте новый проект (или выберите существующий):**
   - Нажмите на выпадающий список проектов вверху
   - Нажмите **"New Project"**
   - Введите название: `MellChat` (или любое другое)
   - Нажмите **"Create"**

3. **Выберите созданный проект:**
   - Убедитесь, что проект выбран в списке проектов

### Шаг 2: Настройте OAuth Consent Screen

1. **Откройте OAuth Consent Screen:**
   - В меню слева: **APIs & Services** → **OAuth consent screen**
   - Или прямой URL: https://console.cloud.google.com/apis/credentials/consent

2. **Выберите User Type:**
   - **External** (для всех пользователей)
   - Нажмите **"Create"**

3. **Заполните информацию о приложении:**
   - **App name**: `MellChat`
   - **User support email**: выберите ваш email
   - **Developer contact information**: ваш email
   - Нажмите **"Save and Continue"**

4. **Scopes (можно пропустить):**
   - Нажмите **"Save and Continue"**

5. **Test users (для разработки):**
   - Добавьте ваш email в список тестовых пользователей
   - Это нужно, пока приложение в режиме тестирования
   - Нажмите **"Save and Continue"**

6. **Summary:**
   - Проверьте информацию
   - Нажмите **"Back to Dashboard"**

### Шаг 3: Создайте OAuth 2.0 Credentials

1. **Откройте Credentials:**
   - В меню слева: **APIs & Services** → **Credentials**
   - Или прямой URL: https://console.cloud.google.com/apis/credentials

2. **Создайте OAuth Client ID:**
   - Нажмите **"+ CREATE CREDENTIALS"**
   - Выберите **"OAuth client ID"**

3. **Выберите Application type:**
   - **Web application**

4. **Заполните данные:**
   - **Name**: `MellChat Web Client`
   
   - **Authorized JavaScript origins:**
     ```
     http://localhost:5173
     http://localhost:3000
     ```
     (Добавьте ваш production URL, когда будете деплоить)
   
   - **Authorized redirect URIs:**
     ```
     http://localhost:3001/api/v1/auth/google/callback
     ```
     (Для production замените на ваш домен)

5. **Создайте:**
   - Нажмите **"CREATE"**

6. **Скопируйте данные:**
   - Появится модальное окно с:
     - **Your Client ID** (например: `123456789-abc.apps.googleusercontent.com`)
     - **Your Client Secret** (например: `GOCSPX-abc123def456`)
   - **Сохраните их!** Они понадобятся в следующем шаге

### Шаг 4: Настройте переменные окружения

Откройте файл `.env` в `backend/api-gateway/` и добавьте:

```bash
# OAuth Configuration (Google)
GOOGLE_CLIENT_ID=ваш-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-ваш-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/v1/auth/google/callback

# Frontend URL (for OAuth redirects)
FRONTEND_URL=http://localhost:5173
```

**Пример:**
```bash
GOOGLE_CLIENT_ID=387174087965-jh1hmdfs8hs6k6j443npl9tu7ndstl8q.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-ваш-client-secret-здесь
GOOGLE_CALLBACK_URL=http://localhost:3001/api/v1/auth/google/callback
FRONTEND_URL=http://localhost:5173
```

**Где найти Client Secret:**
1. Откройте: https://console.cloud.google.com/apis/credentials
2. Найдите ваш OAuth 2.0 Client ID: `387174087965-jh1hmdfs8hs6k6j443npl9tu7ndstl8q.apps.googleusercontent.com`
3. Нажмите на него
4. Скопируйте значение в поле **"Client secret"** (начинается с `GOCSPX-`)

### Шаг 5: Перезапустите сервер

```bash
cd backend/api-gateway
npm run dev
```

### Шаг 6: Проверьте работу

1. Откройте приложение в браузере
2. Нажмите **"Войти через Google"**
3. Должен открыться Google OAuth экран
4. После подтверждения вы будете перенаправлены обратно в приложение

---

## 🔧 Настройка для Production

### 1. Опубликуйте приложение в Google

1. **В OAuth Consent Screen:**
   - Нажмите **"PUBLISH APP"**
   - Это сделает приложение доступным для всех пользователей
   - **Внимание:** Это уберет ограничение на тестовых пользователей

### 2. Обновите Authorized URIs

В Google Cloud Console → Credentials:

**Authorized JavaScript origins:**
```
https://yourdomain.com
https://www.yourdomain.com
```

**Authorized redirect URIs:**
```
https://api.yourdomain.com/api/v1/auth/google/callback
```

### 3. Обновите .env для Production

```bash
GOOGLE_CLIENT_ID=ваш-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-ваш-client-secret
GOOGLE_CALLBACK_URL=https://api.yourdomain.com/api/v1/auth/google/callback
FRONTEND_URL=https://yourdomain.com
```

---

## ⚠️ Troubleshooting

### Ошибка: "redirect_uri_mismatch"

**Причина:** Redirect URI в Google Console не совпадает с тем, что указан в коде.

**Решение:**
1. Проверьте `GOOGLE_CALLBACK_URL` в `.env`
2. Проверьте **Authorized redirect URIs** в Google Console
3. Они должны совпадать **точно** (включая `http://` vs `https://`, порты, слэши)

### Ошибка: "access_denied"

**Причина:** Пользователь не в списке тестовых пользователей (в режиме тестирования).

**Решение:**
1. Откройте OAuth Consent Screen
2. Добавьте email пользователя в **Test users**
3. Или опубликуйте приложение (PUBLISH APP)

### Ошибка: "invalid_client"

**Причина:** Неверный `GOOGLE_CLIENT_ID` или `GOOGLE_CLIENT_SECRET`.

**Решение:**
1. Проверьте, что скопировали правильные значения из Google Console
2. Убедитесь, что нет лишних пробелов
3. Перезапустите сервер после изменения `.env`

### Ошибка: "Error 400: redirect_uri_mismatch"

**Причина:** Frontend URL не совпадает с Authorized JavaScript origins.

**Решение:**
1. Проверьте `FRONTEND_URL` в `.env`
2. Проверьте **Authorized JavaScript origins** в Google Console
3. Они должны совпадать

---

## 🔒 Безопасность

### Важные моменты:

1. **Не коммитьте .env файл в git!**
   - `.env` должен быть в `.gitignore`

2. **Client Secret - это секретная информация:**
   - Никогда не публикуйте его в открытом доступе
   - Не передавайте через незащищенные каналы

3. **Для Production используйте переменные окружения сервера:**
   - Не храните секреты в файлах на production сервере
   - Используйте секреты из настроек хостинга (Heroku, Vercel, AWS и т.д.)

4. **Ограничьте Authorized URIs:**
   - Указывайте только ваши домены
   - Не используйте `*` в URI

---

## 📝 Проверка настройки

После настройки проверьте:

1. **Логи бэкенда:**
   ```
   ✅ Google OAuth configured
   ```

2. **В браузере:**
   - Нажмите "Войти через Google"
   - Должен открыться Google OAuth экран
   - После входа вы должны быть авторизованы

3. **В консоли бэкенда при успешном входе:**
   ```
   ✅ User authenticated: user@gmail.com
   ```

---

## 🔄 Обновление настроек

Если нужно изменить Client ID или Secret:

1. Создайте новые credentials в Google Console
2. Обновите `.env` файл
3. Перезапустите сервер
4. Старые credentials можно удалить (но лучше оставить для отката)

---

## 📚 Дополнительные ресурсы

- **Google OAuth 2.0 Documentation**: https://developers.google.com/identity/protocols/oauth2
- **Google Cloud Console**: https://console.cloud.google.com
- **OAuth 2.0 Playground** (для тестирования): https://developers.google.com/oauthplayground

