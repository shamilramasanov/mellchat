# 🔥 Railway Infinite Restart - Финальное решение

## Проблема

Приложение **крашится в бесконечном цикле** каждую секунду:
- Миграции проходят успешно ✅
- Rate limiter инициализируется ✅
- Redis подключается ✅
- **СЕРВЕР НЕ ЗАПУСКАЕТСЯ** ❌

## Причина

**`apply-migrations.js` - это НЕ модуль, а скрипт!**

Когда мы убрали `process.exit(0)`, скрипт миграций завершается, но:
1. Скрипт асинхронный (`async function runMigrations()`)
2. Вызывается `runMigrations()` без `await` в конце файла
3. Node.js завершает процесс **ДО** завершения миграций!
4. После завершения миграций пытается запустить `node src/index.js`
5. Но миграции еще **не закончены**, скрипт крашится
6. Railway перезапускает контейнер
7. **Бесконечный цикл!**

## Логи

Видно множественные запуски каждую секунду:
```
04:52:57 - Migration #1
04:52:58 - Migration #2  
04:53:00 - Migration #3
04:53:02 - Migration #4
04:53:04 - Migration #5
... бесконечно
```

## Решение

### Вариант 1: Добавить `await` в apply-migrations.js (РЕКОМЕНДУЕТСЯ)

Файл `backend/api-gateway/apply-migrations.js`, строка 227:

**БЫЛО:**
```javascript
runMigrations();
```

**СТАЛО:**
```javascript
runMigrations().catch(error => {
  console.error('Migration error:', error);
  process.exit(1);
});
```

НО! Скрипт все равно завершится после миграций, и `node src/index.js` НЕ запустится!

### Вариант 2: СДЕЛАТЬ МИГРАЦИИ МОДУЛЕМ (ПРАВИЛЬНО!)

**Создать `backend/api-gateway/scripts/run-migrations.js`:**

```javascript
#!/usr/bin/env node

const { Client } = require('pg');
const fs = require('fs');
const path = require('path');

async function runMigrations() {
  // ... весь код из apply-migrations.js
}

runMigrations().then(() => {
  console.log('Migrations completed successfully');
  process.exit(0);
}).catch(error => {
  console.error('Migration failed:', error);
  process.exit(1);
});
```

**Обновить `package.json`:**

```json
{
  "scripts": {
    "start:with-migrations": "node scripts/run-migrations.js && node src/index.js"
  }
}
```

### Вариант 3: ИСПРАВИТЬ ПРИЧИНУ (ЛУЧШЕЕ!)

**Проблема:** Скрипт `apply-migrations.js` выполняется **ДВАЖДЫ**!

Смотри логи:
```
04:52:57 - Первый запуск миграций
04:53:01 - Второй запуск миграций  
04:53:05 - Третий запуск миграций
```

**Причина:** Railway запускает несколько экземпляров контейнера!

**Проверьте в Railway Dashboard:**
1. Settings → Deployments
2. Сколько контейнеров запущено?
3. Health checks настроены?

## Правильное решение

### 1. Исправить apply-migrations.js

```javascript
// В конец файла добавить:
(async () => {
  try {
    await runMigrations();
    // НЕ выходим, позволяем следующей команде запуститься
  } catch (error) {
    console.error('Migration failed:', error);
    process.exit(1);
  }
})();
```

### 2. Проверить Railway конфигурацию

**Проблема может быть в:**

1. **Health check падает**
   - Railway думает что приложение мертво
   - Перезапускает каждую секунду
   
2. **Множественные инстансы**
   - Случайно создали несколько сервисов
   - Каждый пытается стартовать

3. **Неправильный startCommand**
   - Проверьте `railway.json` в `backend/api-gateway/`

## Быстрое решение

В Railway Dashboard:

1. **Settings** → **Deployments** → **Scale**
2. Установите **1 instance**
3. **Settings** → **Health Check**
4. Отключите health check (временно)
5. Перезапустите сервис

## Проверка

После исправления должны увидеть в логах:

```
✅ Connected to database on attempt 1
📝 Running migrations...
🎉 Migrations completed!
   ✅ Applied: 0
   ⏭️  Skipped: 20
✅ All routes loaded successfully  ← ЭТО ДОЛЖНО БЫТЬ!
✅ API Gateway started successfully on 0.0.0.0:3001 ← И ЭТО!
```

**БЕЗ МНОЖЕСТВЕННЫХ ЗАПУСКОВ!**

## Если ничего не помогает

**Полный сброс:**

1. Railway Dashboard → Service → **Settings** → **Delete Service**
2. "+ New" → **"Empty Service"**
3. GitHub → **"shamilramasanov/mellchat"**
4. **Root Directory:** `backend/api-gateway`
5. **Variables:** Добавьте все `DATABASE_URL`, `REDIS_URL` и т.д.
6. **Deploy**

## Финальный чеклист

- [ ] Root Directory = `backend/api-gateway`
- [ ] Только 1 instance (не multiple)
- [ ] Health check отключен или настроен правильно
- [ ] `apply-migrations.js` не завершает процесс
- [ ] `src/index.js` запускается после миграций
- [ ] Нет множественных логов миграций
- [ ] Сервер стартует и слушает порт
- [ ] Health endpoint отвечает

---
**Главная проблема:** Railway запускает контейнер в бесконечном цикле, потому что либо:
1. Health check считает приложение мертвым
2. Миграции крашат процесс до запуска сервера
3. Множественные инстансы

**Решение:** Фиксим миграции + проверяем количество инстансов.

