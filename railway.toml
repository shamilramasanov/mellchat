[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend/api-gateway && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
installCommand = "cd backend/api-gateway && npm install"

