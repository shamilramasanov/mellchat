[build]
builder = "NIXPACKS"

[build.env]
NODE_VERSION = "18"
CI = "false"

[deploy]
startCommand = "cd backend/api-gateway && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
