# 🚀 Deployment Guide - MellChat

Инструкция по деплою MellChat на Railway (Backend) и Vercel (Frontend).

## 📋 Предварительные требования

1. **GitHub Repository** - код должен быть в публичном или приватном репозитории
2. **Railway Account** - для бэкенда (https://railway.app)
3. **Vercel Account** - для фронтенда (https://vercel.com)
4. **Переменные окружения** - собраны из `backend/api-gateway/env.example`

---

## 🔧 Backend Deployment (Railway)

### Шаг 1: Создание проекта на Railway

1. Зайдите на https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `MellChat`
5. Выберите папку `backend/api-gateway`

### Шаг 2: Добавление сервисов

#### PostgreSQL Database
1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database" → "PostgreSQL"**
3. Railway автоматически создаст БД и добавит переменную `DATABASE_URL`

#### Redis (опционально, но рекомендуется)
1. Нажмите **"+ New"**
2. Выберите **"Database" → "Redis"**
3. Railway автоматически создаст Redis и добавит переменную `REDIS_URL`

### Шаг 3: Настройка переменных окружения

В настройках вашего сервиса добавьте все переменные из `backend/api-gateway/env.example`:

#### Обязательные:
```bash
NODE_ENV=production
PORT=3001
JWT_SECRET=<сгенерируйте_секретный_ключ>
ADMIN_USERNAME=admin
ADMIN_PASSWORD=<надежный_пароль>
ADMIN_EMAIL=admin@mellchat.live
```

#### База данных (Railway создаст автоматически):
```bash
DATABASE_URL=<автоматически_создается_railway>
REDIS_URL=<автоматически_создается_railway>
```

#### OAuth (Google):
```bash
GOOGLE_CLIENT_ID=<ваш_google_client_id>
GOOGLE_CLIENT_SECRET=<ваш_google_client_secret>
GOOGLE_CALLBACK_URL=https://<ваш_railway_domain>/api/v1/auth/google/callback
FRONTEND_URL=https://<ваш_vercel_domain>
```

#### External APIs:
```bash
TWITCH_CLIENT_ID=<ваш_twitch_client_id>
TWITCH_CLIENT_SECRET=<ваш_twitch_client_secret>
YOUTUBE_API_KEY=<ваш_youtube_api_key>
GEMINI_API_KEY=<ваш_gemini_api_key>
```

#### CORS:
```bash
CORS_ORIGIN=https://<ваш_vercel_domain>
```

### Шаг 4: Настройка деплоя

1. Railway автоматически обнаружит `package.json` в `backend/api-gateway`
2. Убедитесь, что в настройках Build:
   - **Root Directory**: `backend/api-gateway`
   - **Build Command**: `npm install && npm run migrate`
   - **Start Command**: `npm start`

### Шаг 5: Применение миграций

Миграции запустятся автоматически через `npm run migrate` в build команде.
Если нужно применить вручную, используйте Railway CLI или SSH.

### Шаг 6: Получение URL

После успешного деплоя Railway предоставит URL вида:
```
https://mellchat-production.up.railway.app
```

**Скопируйте этот URL** - он понадобится для настройки фронтенда.

---

## 🎨 Frontend Deployment (Vercel)

### Шаг 1: Импорт проекта в Vercel

1. Зайдите на https://vercel.com
2. Нажмите **"Add New..." → "Project"**
3. Импортируйте репозиторий `MellChat` из GitHub
4. Настройте проект:
   - **Framework Preset**: `Vite`
   - **Root Directory**: `frontend/pwa`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### Шаг 2: Обновление vercel.json

Откройте `frontend/pwa/vercel.json` и обновите URL бэкенда:

```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://<ваш_railway_domain>/api/$1"
    }
  ],
  "env": {
    "VITE_API_URL": "https://<ваш_railway_domain>",
    "VITE_WS_URL": "wss://<ваш_railway_domain>"
  }
}
```

Замените `<ваш_railway_domain>` на URL из Railway (например, `mellchat-production.up.railway.app`).

### Шаг 3: Переменные окружения в Vercel

В настройках проекта Vercel → **Settings → Environment Variables** добавьте:

```bash
VITE_API_URL=https://<ваш_railway_domain>
VITE_WS_URL=wss://<ваш_railway_domain>
VITE_APP_NAME=MellChat
VITE_APP_VERSION=2.0.1
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=false
```

### Шаг 4: Деплой

1. **Автоматический**: При пуше в `main` ветку Vercel автоматически задеплоит изменения
2. **Ручной**: Нажмите **"Deploy"** в интерфейсе Vercel

---

## ✅ Проверка деплоя

### Backend Health Check
```bash
curl https://<ваш_railway_domain>/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": "...",
  "uptime": "..."
}
```

### Frontend Check
Откройте URL от Vercel в браузере и убедитесь, что:
1. Приложение загружается
2. WebSocket подключается (проверьте консоль браузера)
3. Можно открыть стрим

---

## 🔄 Автоматический деплой через GitHub

### Railway (Backend)

Railway автоматически деплоит при пуше в `main` ветку, если:
1. Проект подключен к GitHub репозиторию
2. Настроен автоматический деплой в настройках Railway

### Vercel (Frontend)

Vercel автоматически деплоит при пуше в `main` ветку по умолчанию.

---

## 🐛 Troubleshooting

### Backend не запускается на Railway

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `DATABASE_URL` и `REDIS_URL` правильно настроены
4. Убедитесь, что порт указан правильно (Railway автоматически устанавливает `PORT`)

### Frontend не подключается к Backend

1. Проверьте CORS настройки в Railway (переменная `CORS_ORIGIN`)
2. Убедитесь, что `VITE_API_URL` и `VITE_WS_URL` правильные в Vercel
3. Проверьте, что Railway сервис работает (Health Check)
4. Проверьте браузерную консоль на ошибки CORS или WebSocket

### Миграции не применяются

1. Проверьте логи build в Railway
2. Убедитесь, что скрипт `migrate` существует в `package.json`
3. Примените миграции вручную через Railway CLI:
   ```bash
   railway run npm run migrate
   ```

### WebSocket не работает

1. Убедитесь, что используете `wss://` (не `ws://`) для продакшена
2. Проверьте, что Railway поддерживает WebSocket (по умолчанию поддерживает)
3. Проверьте настройки WebSocket в `backend/api-gateway/src/ws/server.js`

---

## 📝 Чек-лист перед деплоем

- [ ] Все переменные окружения добавлены в Railway
- [ ] База данных PostgreSQL создана в Railway
- [ ] Redis создан в Railway (опционально)
- [ ] Миграции применены
- [ ] `vercel.json` обновлен с правильным URL бэкенда
- [ ] Переменные окружения добавлены в Vercel
- [ ] Health check проходит успешно
- [ ] Тесты проходят локально
- [ ] Git репозиторий синхронизирован

---

## 🚀 После успешного деплоя

1. Протестируйте основные функции:
   - Открытие стрима
   - Получение сообщений
   - Авторизация
   - Админ панель

2. Настройте кастомный домен (опционально):
   - Railway: Settings → Domains
   - Vercel: Settings → Domains

3. Включите мониторинг:
   - Railway предоставляет метрики по умолчанию
   - Vercel предоставляет аналитику в дашборде

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в Railway Dashboard
2. Проверьте логи в Vercel Dashboard
3. Проверьте консоль браузера для ошибок фронтенда
4. Проверьте Network tab в браузере для проблем с API

---

**Успешного деплоя! 🎉**

